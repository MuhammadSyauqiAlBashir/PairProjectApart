<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Apartement Syariah</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
  </head>
<body style="background-image: url(https://media.dekoruma.com/dekohouse/property/real-estate/INTL-ASQR/tower/apartemen-south-quarter-tb-simatupang-jakarta-selatan-tower-d-tower2.jpg?bg-color=FFFFFF&dpr=1.5&fit=crop&height=412&optimize=medium&quality=50&width=412); background-size: cover;">
  <%- include('./partials/_navbar') %>
  <% if (!user.Profile) { %>
    <div class="container mt-5 card">
      <h1 class="text-center mt-4">Fill your profile first</h1>
      <div class="row justify-content-center">
        <div class="col-md-6">
          <div>
            <%if (error.length > 0 ){%>
              <%error.forEach((item) => {%>
                <h5 class="btn btn-danger"><%=item%></h5>
              <%})%>
            <%}%>
          </div>
              <form action="/<%= user.role %>" method="post">
                <div class="row g-3">
                  <div class="form-floating mb-3 col">
                    <input type="text" class="form-control" id="floatingInput" placeholder="Name" name="name">
                    <label for="floatingInput">Name</label>
                  </div>
                  <div class="col mb-3">  
                      <select class="form-select" style="height: 60px;" id="gender" name="gender">
                          <option value="" disabled selected>Select Gender</option>
                          <option value="male">Male</option>
                          <option value="female">Female</option>
                      </select>
                  </div>
                </div>
                  <div class="form-floating mb-3"">
                      <input type="number" class="form-control" id="nik" name="nik">
                      <label for="nik" class="form-label">NIK</label>
                  </div>
                  <div class="mb-3">
                      <label for="birthDate" class="form-label">Birth Date</label>
                      <input type="date" class="form-control" id="birthDate" name="birthDate">
                  </div>
                  <button type="submit" class="btn btn-primary mb-4">Update Profile</button>
              </form>
          </div>
      </div>
  </div>
  <% } else { %>
    <div class="container mt-5 alert alert-success">
      <div class="row justify-content-center">
        <h1 class="text-center">Assalamualaikum, <%= user.role %> <%= user.Profile.name %> <br>Silahkan Pilih Menu Apartement di Atas untuk Menampilkan Daftar Apartement</h1>
      </div>
    </div>
  <% } %>
</body>
</html>